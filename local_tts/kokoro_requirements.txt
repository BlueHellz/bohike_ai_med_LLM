# Optional Kokoro-82M dev TTS (Python 3.10–3.13; onnxruntime, no torch).
# Install: pip install -r local_tts/kokoro_requirements.txt
# Or after setup: ./scripts/setup_kokoro_tts.sh
kokoro-onnx>=0.4.0
soundfile>=0.12.1
